# Cloud Build CD Pipeline for Dataflow Project
# This pipeline deploys the Dataflow job to Google Cloud
# Triggered manually or on merges to main branch

substitutions:
  _PROJECT_ID: 'your-gcp-project-id'
  _REGION: 'us-central1'
  _TEMP_LOCATION: 'gs://your-bucket/temp'
  _STAGING_LOCATION: 'gs://your-bucket/staging'
  _OUTPUT_LOCATION: 'gs://your-bucket/output'
  _SERVICE_ACCOUNT: 'dataflow-sa@your-gcp-project-id.iam.gserviceaccount.com'
  _NETWORK: 'default'
  _SUBNETWORK: 'regions/us-central1/subnetworks/default'

steps:
  # Step 1: Install dependencies
  - name: 'python:3.9'
    id: 'install-dependencies'
    entrypoint: 'pip'
    args: ['install', '-r', 'requirements.txt', '--user']

  # Step 2: Run CI checks before deployment
  - name: 'python:3.9'
    id: 'pre-deployment-checks'
    entrypoint: 'python'
    args: ['-m', 'flake8', 'main.py', '--max-line-length=100']
    waitFor: ['install-dependencies']

  # Step 3: Deploy to Dataflow
  - name: 'python:3.9'
    id: 'deploy-dataflow'
    entrypoint: 'python'
    args:
      - 'main.py'
      - '--runner=DataflowRunner'
      - '--project=${_PROJECT_ID}'
      - '--region=${_REGION}'
      - '--temp_location=${_TEMP_LOCATION}'
      - '--staging_location=${_STAGING_LOCATION}'
      - '--output=${_OUTPUT_LOCATION}/wordcount'
      - '--service_account_email=${_SERVICE_ACCOUNT}'
      - '--network=${_NETWORK}'
      - '--subnetwork=${_SUBNETWORK}'
      - '--job_name=dataflow-sample-${SHORT_SHA}'
      - '--setup_file=./setup.py'
      - '--save_main_session'
    waitFor: ['pre-deployment-checks']
    env:
      - 'GOOGLE_CLOUD_PROJECT=${_PROJECT_ID}'

  # Step 4: Verify deployment
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'verify-deployment'
    entrypoint: 'gcloud'
    args:
      - 'dataflow'
      - 'jobs'
      - 'list'
      - '--region=${_REGION}'
      - '--filter=name:dataflow-sample-${SHORT_SHA}'
      - '--format=table(id,name,type,state)'
    waitFor: ['deploy-dataflow']

# Cloud Build options
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'N1_HIGHCPU_8'

# Timeout for the entire build (30 minutes)
timeout: '1800s'

# Available images will be stored in Artifact Registry
images: []
