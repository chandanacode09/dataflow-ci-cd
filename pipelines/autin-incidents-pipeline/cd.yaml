# Auto-generated CD configuration for autin-incidents-pipeline
# This file defines the continuous deployment workflow for streaming pipeline

steps:
  # Install dependencies
  - name: 'python:3.9'
    id: 'install-dependencies'
    entrypoint: bash
    args:
      - '-c'
      - |
        cd pipelines/autin-incidents-pipeline
        pip install -r requirements.txt

  # Deploy streaming pipeline to Dataflow
  - name: 'python:3.9'
    id: 'deploy-dataflow-streaming'
    entrypoint: bash
    args:
      - '-c'
      - |
        cd pipelines/autin-incidents-pipeline

        echo "Deploying autin-incidents-pipeline streaming pipeline to Dataflow..."

        python main.py \
          --runner DataflowRunner \
          --project axiomatic-robot-458302-r0 \
          --region us-central1 \
          --temp_location gs://axiomatic-robot-458302-r0-dataflow-temp/autin-incidents-pipeline/ \
          --staging_location gs://axiomatic-robot-458302-r0-dataflow-staging/autin-incidents-pipeline/ \
          --input_table axiomatic-robot-458302-r0:austin_incidents.incidents_2008 \
          --output_table axiomatic-robot-458302-r0:austin_incidents.processed_austin_incidents \
          --window_duration 60 \
          --job_name autin-incidents-pipeline-${SHORT_SHA} \
--max_num_workers 4 \--machine_type n1-standard-2 \--disk_size_gb 50 \          --service_account_email dataflow-sa@axiomatic-robot-458302-r0.iam.gserviceaccount.com \
          --streaming \
          --enable_streaming_engine \
          --setup_file ./setup.py

# Service account with Dataflow permissions
serviceAccount: 'dataflow-sa@axiomatic-robot-458302-r0.iam.gserviceaccount.com'

# Timeout (streaming deployments can take longer)
timeout: '1800s'

# Options
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'N1_HIGHCPU_8'

# Substitutions (provided by trigger)
substitutions:
  _DATASET_ID: 'austin_incidents'
  _SOURCE_TABLE: 'incidents_2008'
  _DEST_TABLE: 'processed_austin_incidents'
  _WINDOW_DURATION: '60'